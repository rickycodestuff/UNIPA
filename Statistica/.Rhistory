# 1 - Importa il dataframe "Cpus_data"
# la funzione read.csv() resistuisce gia' un dataframe quindi non abbiamo
# bisogno di usare la funzione data.frame()
cpus_data <- read.csv2("esercizi slide parte5/Cpus_data.csv")
# 2 - Trova i percentili 0.1, 0.22, 0.4, 0.75 e 0.99 della variabile
# "Recommended Customer Price"
percentile <- quantile(cpus_data$Recommended_Customer_Price,
c(0.1, 0.22, 0.4, 0.75, 0.99))
# 3 - Dividi la distribuzione in classi utilizzando i percentili trovati in precedenza
# quindi creaiamo una nuova colonna nel data set che avra' le classi di
# "Reccomended Customer Price"
cpus_data$cutRCP <-
cut(cpus_data$Recommended_Customer_Price,
breaks =
# questo break fara' in modo che il primo intervallo sara'
# [ valore minimo della colonna, primo valore dei percentili ]
# ] secondo valore dei percentili, terzo valore dei percentili ]
# etc etc fino a
# ] ultimo valore dei percentili, valore massimo della colonna ]
c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
),
include.lowest = T
)
avg_og <- sum(cpus_data$Recommended_Customer_Price) / n
# freq. assoluta e freq. assoluta cumulata
freq_ass <- table(cpus_data$cutRCP)
cumsum(freq_ass)
# freq. relativa e freq. relativa cumulata
n <- length(cpus_data$cutRCP)
freq_rel <- freq_ass / n
cumsum(freq_rel)
# media sulla distrubuzione originale
avg_og <- sum(cpus_data$Recommended_Customer_Price) / n
mean(cpus_data$Recommended_Customer_Price)
classes <- c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
)
valori <- c(
min(df$Recommended_Customer_Price),
percentile,
max(df$Recommended_Customer_Price)
)
rm(classes)
valori <- c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
)
centrodi <- (valori[1:6] + valori[2:7])/2
valori[1:6]
valori[1:6] + valori[2:7]
valori[1:6]
valori[2:7]
valori[1:6] + valori[2:7]
classes(valori[1:6])
class(valori[1:6])
is.array(valori[1:6])
is.array(valori)
is.vector(valori[1:6])
is.vector(valori)
a <- c(1, 2, 3)
is.vector(a)
a + b
b <- c(4, 5, 6)
a + b
b <- c(4, 5, 6, 7)
a + b
a + b
b <- c(4, 5, 6, 7, 8)
a + b
b <- c(4, 5, 6, 7, 8, 9)
a + b
c < - c(1, 2, 3)
c <- c(1, 2, 3)
d <- c(1, 2, 3, 4)
a + b
c + d
median(classes)
classes <- c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
)
median(classes)
a <- c(1, 2)
a + b
median(classes[1:2])
for (i in 1:length(classes)) { median(classes[i:i+1]) }
for (i in 1:length(classes)) { print(median(classes[i:i+1])) }
length(classes)
for (i in 1:length(classes) - 1) { print(median(classes[i:i+1])) }
for (i in 1:length(classes) - 1) {
centroidi <- median(classes[i:i+1])
}
rm(ls())
rm(ls())
rm(list=ls())
for (i in 1:length(classes) - 1) {
centroids <- median(classes[i:i+1])
}
# 1 - Importa il dataframe "Cpus_data"
# la funzione read.csv() resistuisce gia' un dataframe quindi non abbiamo
# bisogno di usare la funzione data.frame()
cpus_data <- read.csv2("esercizi slide parte5/Cpus_data.csv")
# 2 - Trova i percentili 0.1, 0.22, 0.4, 0.75 e 0.99 della variabile
# "Recommended Customer Price"
percentile <- quantile(cpus_data$Recommended_Customer_Price,
c(0.1, 0.22, 0.4, 0.75, 0.99))
# 3 - Dividi la distribuzione in classi utilizzando i percentili trovati in precedenza
# quindi creaiamo una nuova colonna nel data set che avra' le classi di
# "Reccomended Customer Price"
cpus_data$cutRCP <-
cut(cpus_data$Recommended_Customer_Price,
breaks =
# questo break fara' in modo che il primo intervallo sara'
# [ valore minimo della colonna, primo valore dei percentili ]
# ] secondo valore dei percentili, terzo valore dei percentili ]
# etc etc fino a
# ] ultimo valore dei percentili, valore massimo della colonna ]
c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
),
include.lowest = T
)
# 4 - Calcola le frequenze assolute e relative cumulate
# freq. assoluta e freq. assoluta cumulata
freq_ass <- table(cpus_data$cutRCP)
cumsum(freq_ass)
# freq. relativa e freq. relativa cumulata
n <- length(cpus_data$cutRCP)
freq_rel <- freq_ass / n
cumsum(freq_rel)
# Calcola la media della variabile "Recommended Customer Price"
# media sulla distrubuzione originale
mean(cpus_data$Recommended_Customer_Price)
# media sulla distrubuzione per classi
# riprendiamo i valori ci ciascuna classe
classes <- c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
)
for (i in 1:length(classes) - 1) {
centroids <- median(classes[i:i+1])
}
rm(n)
freq_rel <- freq_ass / nrows(cpus_data$cutRCP)
freq_rel <- freq_ass / nrow(cpus_data$cutRCP)
cumsum(freq_rel)
cumsum(freq_rel)
nrow(cpus_data$cutRCP)
nrow(cpus_data)
freq_rel <- freq_ass / nrow(cpus_data)
cumsum(freq_rel)
# 1 - Importa il dataframe "Cpus_data"
# la funzione read.csv() resistuisce gia' un dataframe quindi non abbiamo
# bisogno di usare la funzione data.frame()
cpus_data <- read.csv2("esercizi slide parte5/Cpus_data.csv")
# 2 - Trova i percentili 0.1, 0.22, 0.4, 0.75 e 0.99 della variabile
# "Recommended Customer Price"
percentile <- quantile(cpus_data$Recommended_Customer_Price,
c(0.1, 0.22, 0.4, 0.75, 0.99))
# 3 - Dividi la distribuzione in classi utilizzando i percentili trovati in precedenza
# quindi creaiamo una nuova colonna nel data set che avra' le classi di
# "Reccomended Customer Price"
cpus_data$cutRCP <-
cut(cpus_data$Recommended_Customer_Price,
breaks =
# questo break fara' in modo che il primo intervallo sara'
# [ valore minimo della colonna, primo valore dei percentili ]
# ] secondo valore dei percentili, terzo valore dei percentili ]
# etc etc fino a
# ] ultimo valore dei percentili, valore massimo della colonna ]
c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
),
include.lowest = T
)
# 4 - Calcola le frequenze assolute e relative cumulate
# freq. assoluta e freq. assoluta cumulata
freq_ass <- table(cpus_data$cutRCP)
cumsum(freq_ass)
# freq. relativa e freq. relativa cumulata
freq_rel <- freq_ass / nrow(cpus_data)
cumsum(freq_rel)
# Calcola la media della variabile "Recommended Customer Price"
# media sulla distrubuzione originale
mean(cpus_data$Recommended_Customer_Price)
# media sulla distrubuzione per classi
# riprendiamo i valori ci ciascuna classe
classes <- c(
min(cpus_data$Recommended_Customer_Price),
percentile,
max(cpus_data$Recommended_Customer_Price)
)
for (i in 1:length(classes) - 1) {
centroids <- median(classes[i:i+1])
}
freq_ass * centroids
freq_ass * centroids / freq_ass
weighted.mean(centroids, table(cpus_data$cutRCP))
length(centroids)
# calcoliamo ora la mediana ci ciascuna classe, ovvero i centroidi
centroids = (classes[1:6] + classes[2:7]) / 2
centroids <- median(classes[i:i+1])
# calcoliamo ora la mediana ci ciascuna classe, ovvero i centroidi
centroids <- (classes[1:6] + classes[2:7]) / 2
# una volta trovati i centroidi i calcolo e' semplice
# sommatoria di (frequenza assoluta * centroide) / centroide
(freq_ass * centroids) / centroids
weighted.mean(centroids, table(cpus_data$cutRCP))
weighted.mean(centroids, table(cpus_data$cutRCP))
# una volta trovati i centroidi i calcolo e' semplice
# sommatoria di (frequenza assoluta * centroide) / centroide
sum(freq_ass * centroids) / sum(centroids)
# una volta trovati i centroidi i calcolo e' semplice
# sommatoria di (frequenza assoluta * centroide) / centroide
sum(freq_ass * centroids) / centroids
# 6 - Rappresenta i boxplot della variabile "Recommended Customer Price"
# condizionata a "Product Collection"
boxplot(cpus_data$Recommended_Customer_Price~cpus_data$Product_Collection)
library(dplyr)
library(ggplot2)
# 6 - Rappresenta i boxplot della variabile "Recommended Customer Price"
# condizionata a "Product Collection"
boxplot(cpus_data$Recommended_Customer_Price~cpus_data$Product_Collection)
cpus_data |>
ggplot(
aes( x = Product_Collection,
y = Recommended_Customer_Price)
) +
geom_boxplot() + theme_minimal()
# 6 - Rappresenta i boxplot della variabile "Recommended Customer Price"
# condizionata a "Product Collection"
boxplot(cpus_data$Recommended_Customer_Price~cpus_data$Product_Collection)
cpus_data |>
ggplot(
aes( x = Product_Collection,
y = Recommended_Customer_Price)
) +
geom_boxplot() + theme_minimal()
# 1)  Importa il dataframe "Cpus data"
df <- read.csv2("esercizi slide parte5/Cpus_data.csv")
# 2) Trova il percentili 0.1, 0.22, 0.4, 0.75 e 0.99 della variabile Recommended_Customer_Price
percentili <- quantile(df$Recommended_Customer_Price,c(0.1,0.22,0.4,0.75,0.99))
# 3) Dividi la distribuzione in classi utilizzando i percentili trovati in precedenza.
df$cutRCP <-
cut(df$Recommended_Customer_Price,
breaks = c(min(df$Recommended_Customer_Price),
percentili,
max(df$Recommended_Customer_Price)),
include.lowest = T)
# 4)  Calcole le frequenze assolute e relative cumulate
# freq ass
table(df$cutRCP)
# freq ass cumulate
cumsum(table(df$cutRCP))
# freq relative
table(df$cutRCP)/nrow(df)
# freq relative
cumsum(table(df$cutRCP)/nrow(df))
# 5) Calcola la media della variabili Recommended_Customer_Price prima sulla distribuzione originale
# e poi sulla distribuzione divisa in classi
mean(df$Recommended_Customer_Price)
valori <- c(
min(df$Recommended_Customer_Price),
percentili,
max(df$Recommended_Customer_Price)
)
centrodi <- (valori[1:6] + valori[2:7])/2
weighted.mean(centrodi,table(df$cutRCP))
# 6) rappresenta i boxplot della variabile Recommended_Customer_Price condizionata a Product_Collection
boxplot(df$Recommended_Customer_Price~df$Product_Collection)
library(dplyr)
library(ggplot2)
df |>
ggplot(aes(x=Product_Collection,y=Recommended_Customer_Price)) +
geom_boxplot() + theme_minimal()
df |>
ggplot(aes(x=Product_Collection,y=Recommended_Customer_Price)) +
geom_boxplot() + theme_minimal()
